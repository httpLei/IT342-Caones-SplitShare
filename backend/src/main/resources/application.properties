spring.application.name=splitshare

spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${SUPABASE_DB_USERNAME:postgres.bbohzcgbyuonxlmuspnn}
spring.datasource.password=${SUPABASE_DB_PASSWORD:splitshare2026}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# ── JWT ──────────────────────────────────────────────────────────────────────
# 512-bit Base64-encoded secret (change before deploying to production)
application.security.jwt.secret-key=U3BsaXRTaGFyZVN1cGVyU2VjcmV0Snd0S2V5MjAyNkBDSVRFZHVjYXRpb25fTXVzdEJlNjRCeXRlc0xvbmdGb3JIUzUxMiEh
application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000

# ── Server ───────────────────────────────────────────────────────────────────
server.port=8080
application.upload.dir=uploads

# Admin bootstrap (set to true only when you want to create/promote an admin)
application.admin.bootstrap.enabled=${ADMIN_BOOTSTRAP_ENABLED:true}
application.admin.bootstrap.email=${ADMIN_BOOTSTRAP_EMAIL:admin@gmail.com}
application.admin.bootstrap.password=${ADMIN_BOOTSTRAP_PASSWORD:adminsplitshare}
application.admin.bootstrap.firstname=${ADMIN_BOOTSTRAP_FIRSTNAME:Super}
application.admin.bootstrap.lastname=${ADMIN_BOOTSTRAP_LASTNAME:Admin}
